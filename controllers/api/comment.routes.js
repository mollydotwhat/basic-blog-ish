const router = require('express').Router();
const { User, Post, Comment} = require('../../models');

// get all comments (by post, though?)

// get individual comment

// post a comment

// update a comment?

// delete a comment

module.exports = router;
