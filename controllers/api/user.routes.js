const router = require('express').Router();
const { User, Post, Comment} = require('../../models');


// get all

// get by id

// new user

// ? update user

// ? delete user


module.exports = router;